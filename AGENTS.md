Avoid directly accessing .gradle; instead, proactively use ksrc bash tool to inspect source code of
dependencies to learn API shapes or implementations. Start with ksrc --help.

After you’ve finished editing:

- Use the Android Studio MCP (if available) to find any problems.